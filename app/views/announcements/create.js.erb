<% if @announcement.valid? %>
  hideModal();
  location.reload();
<% else %>
  $("#errorExplanation").remove();
  $("<%=j error_messages_for("announcement") %>").prependTo("#new_announcement");
<% end %>